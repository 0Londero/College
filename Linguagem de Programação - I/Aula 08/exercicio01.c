#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Primreira Questão 
    // P++ = incrementa ou move o ponteiro.
    // (*p)++; = incrementa o conteúdo e não o ponteiro. 
    // *(p++) = incrementa o conteúdo primeiro e depois o ponteiro, altra o conteúdo o qual o ponteiro aponta e após move o ponteiro. 
    // *(p+10) = acessa o valor no décimo elemento após a posição de p , sem modificar a posição original de p
}